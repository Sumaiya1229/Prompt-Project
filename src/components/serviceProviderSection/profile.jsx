import React, { useState } from "react";
import { connect } from "react-redux";
import RequestCard from "./spCommon/requestCard";
import {
  View,
  Text,
  ScrollView,
  Modal,
  TouchableOpacity,
  TextInput,
  Image,
} from "react-native";
const Profile = () => {
  return <Text>This is the profile section</Text>;
};

export default Profile;
